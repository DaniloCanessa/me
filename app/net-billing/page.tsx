import NetBillingClient from './NetBillingClient';

export const metadata = { title: 'Net Billing en Chile — Mercado Energy' };

export default function NetBillingPage() {
  return <NetBillingClient />;
}
